version: '3.8'
services:

  postgres:
    image: postgres:13.5
    restart: always
    environment:
      - POSTGRES_USER=ducanh261101a
      - POSTGRES_PASSWORD=01652149936a
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - '5454:5432'

volumes:
  postgres: